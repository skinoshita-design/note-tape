# STATUS.md — 現在地・詰まりの記録

## 現在地（最終更新：スマホ実機テスト完了時点）

- [x] 仕様検討（`SPEC.md`）
- [x] UI実装（講義名入力／音声ファイル選択／実行ボタン／文字起こし表示／要約表示／コピー機能）
- [x] Gemini API連携（文字起こし・要約の2段階呼び出し）
- [x] APIキーの `localStorage` 保存・設定モーダル実装
- [x] レスポンシブ対応（スマホ幅での崩れなし、実機確認済み）
- [x] 実際の音声ファイルを使った動作確認（PC・iPhoneともに文字起こし成功、要約もPCでは成功）
- [x] GitHub Pagesへのデプロイと公開URLでの最終確認（https://skinoshita-design.github.io/note-tape/）
- [ ] スクリーンショットの取得（`screenshots/` に格納）※本ステータス更新時点で未実施
- [ ] iPhoneでの要約生成の最終確認（下記「6. Gemini無料枠のレート制限」参照。文字起こしは成功済み）

## 詰まった点・判断が必要だった点

### 1. 音声文字起こしAPIの選定（Gemini vs Claude）
- 当初、要約以外の全処理もClaude APIに統一することを検討したが、**Claude APIには音声ファイルを直接入力する機能が無い**ことが判明（テキスト・画像・PDFのみ対応）
- 音声→テキスト化を代替する場合、ブラウザの音声認識機能（Web Speech API）でその場録音する方式に変更する必要があり、「音声ファイルアップロード」という課題の最短導線から外れてしまう
- → 音声ファイルアップロードのまま最短導線を満たすため、**文字起こし・要約とも Gemini API に統一**する方針で確定（`API_NOTES.md` 参照）

### 2. モデル選定
- 当初検討した `gemini-2.0-flash` は2026年6月1日にシャットダウン済みであることが判明
- 無料枠がありマルチモーダル対応の現行モデルとして `gemini-2.5-flash` を採用

### 3. 実機テスト中に発生した404エラー（モデルの早期利用不可）
- 実際に音声を投入してテストしたところ、「This model models/gemini-2.5-flash is no longer available to new users」という404エラーが発生
- `gemini-2.5-flash` の公式な廃止予定日は2026年10月16日だったが、7月上旬から新規利用者に対して先行して404が返る事象がGoogle公式フォーラムでも報告されていた
- 公式ドキュメント（音声理解ページ、2026年5月18日更新）で案内されている `gemini-3.5-flash` に切り替えて解決（`API_NOTES.md` に詳細記載）

### 4. iPhone（Safari）で音声ファイルが選択できない
- iPhoneのファイルアプリで音声ファイル（ボイスメモ由来の`.m4a`）がグレーアウトして選択できない事象が発生
  - 原因：`<input type="file" accept="audio/*">` の`accept`指定だけでは、iOS SafariがボイスメモのMIMEタイプを正しく認識できないケースがある
  - 対応：`accept`属性に拡張子指定（`.m4a` など）とMIMEタイプを併記し、対応範囲を広げて解決
- グレーアウトは解消したが、続けて「ファイルを選択→アプリ画面に戻ると未選択扱いになる」という別の事象が発生
  - 原因：ファイル選択エリアを「JavaScriptで隠れたinputをクリックさせる」実装にしていたため、iOS Safariでイベントの取りこぼしが起きていたと推測
  - 対応：ブラウザ標準の `<label for="audioInput">` に選択エリアを置き換え、JSでのクリック誘発をやめてネイティブの挙動に任せる形に変更。あわせて `change` に加えて `input` イベントも監視するようにして取りこぼしに備えた
- GitHub Pagesで実際に公開（`https://`）した状態でも同じ事象が再発。講義名の入力内容は保持されたままだったため、ページ全体がリロードされているわけではなく、iOS側でファイルの受け渡し自体が失敗していると判断
  - 仮説：iPhoneの「ボイスメモ」アプリが書き出す`.m4a`は、内部的に特殊なUTI（保護されたMPEG-4オーディオ扱い）になっている場合があり、`accept`属性によるOS側フィルタリングとかみ合わず、選択操作自体は通っても`input.files`にファイルが渡らないことがある
  - 対応：`accept`属性による制限を撤廃し、ファイル形式のチェックはOSに任せず、選択後にJavaScript側で緩やかに検証する方式に変更（明らかに音声でないファイル名・MIMEの場合のみ注意メッセージを表示するが、実行はブロックしない）

### 5. GitHub Pagesが正しく公開されない（README.mdが表示される／404）
- 最初のアップロードでファイル一式をzip形式のまま（`files.zip`）アップロードしてしまい、リポジトリ直下に`index.html`が存在せず404エラーが発生
  - 対応：zipを解凍し、中身のファイルを直接アップロードし直して解決
- 再アップロード後も、`index.html`のファイル名がダウンロード時の重複回避で`index_5.html`のようになっており、GitHub Pagesが認識できず、`README.md`から自動生成された簡易ページが表示される事象が発生
  - 対応：GitHub上でファイル名を`index.html`に修正して解決
- 念のため、Jekyll自動処理を無効化する`.nojekyll`（空ファイル）もリポジトリ直下に追加した

### 6. Gemini無料枠のレート制限（429エラー）
- 公開後の実機テストを繰り返した結果、「429: Quota exceeded」エラーが発生（`generate_content_free_tier` の制限に抵触、`limit: 20`）
- 文字起こし自体は成功しており、後続の要約生成リクエストで発生。数十秒〜数分待っても解消しなかったため、無料枠の1日あたりの上限（RPD）に達した可能性が高いと判断
- コード側の不具合ではなくAPI利用量の制約によるものと判断し、**この時点でのスマホ実機テストは「文字起こし成功・要約は翌日以降に再確認」として一旦完了扱い**とする方針にした

### 7. APIキーの扱い
- サーバーを持たない構成のため、APIキーはブラウザの `localStorage` に保存し、ブラウザから直接Gemini APIへ送信する方式を採用
- この構成では、デプロイ先を公開URLにした場合、閲覧者が自分自身のAPIキーを入力して使う前提になる（開発者のキーを埋め込んで公開しない）ことを明記

## 次にやること

1. `TESTCASES.md` のTC01〜TC10について、結果と証跡（スクリーンショット）を`screenshots/`に格納する
2. 無料枠のレート制限が解消したタイミングで、iPhoneでの要約表示までの最終確認を行う
3. 公開リポジトリにAPIキーが直書きされていないか最終確認する
