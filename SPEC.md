# SPEC.md — 仕様書

## 1. アプリ概要

**名称**：ノートテープ（音声文字起こし・要約アプリ）

**目的**：「AIアプリ開発の最初の一歩」課題として、外部API（Gemini）を使い、
入力（音声）→処理（文字起こし／要約）→出力（画面表示）を自力で通すこと。

**スコープ外（今回は実装しない）**：保存（DB／Drive連携）、共有（LINE等）、
編集機能、話者分離、ユーザー認証、サーバーサイド処理。
すべてブラウザ上で完結するクライアントサイドのみの実装。

## 2. ユーザー体験（最短導線）

1. 講義名（任意、推奨）を入力する
2. 音声ファイルをファイル選択で投入する
3. 「実行する」ボタンを押す
4. 文字起こしが画面に表示される
5. 続けて要約（要約文＋要点3〜5個）が画面に表示される
6. 文字起こし・要約それぞれをコピーできる

この6ステップがスマホ（レスポンシブUI）で破綻せず動くことをゴールとする。

## 3. 機能要件

### A. 音声入力（必須）
- 実装方式：音声ファイルアップロード（`<input type="file" accept="audio/*">`）
- ドラッグ&ドロップにも対応
- 推奨：30秒〜2分程度の短い音声
- スマホでも操作できるレスポンシブUI

### B. 文字起こし（必須）
- Gemini API に音声ファイルを送信し、文字起こし本文を生成
- 全文をそのまま表示（編集機能はなし）
- コピーボタンで全文をクリップボードにコピー可能

### C. 要約（必須）
- 文字起こしを入力として、以下を生成
  - 要約（150〜250字程度）
  - 要点（箇条書き3〜5個）
- コピーボタンで「要約＋要点」をまとめてコピー可能

### D. APIキーの扱い（必須）
- APIキーはソースコードに直書きしない
- 初回起動時にユーザーがモーダルでAPIキーを入力し、`localStorage` に保存
- 設定（歯車アイコン）からいつでも変更・削除が可能
- APIキーはブラウザから Gemini API へ直接送信されるのみで、アプリ側のサーバーは存在しない（バックエンドなし）

### E. 取り扱い注意（必須）
- 個人情報・機密情報を含む音声はアップロードしないよう、UI上に注意書きを常時表示
- 音声・文字起こし・要約はどこにも保存されず、ページを離れると消える旨を明記

## 4. 画面構成

| ブロック | 内容 |
|---|---|
| ヘッダー | タイトル、説明文、APIキー設定ボタン（歯車アイコン） |
| 入力カード | 講義名入力欄、音声ファイル選択（ドラッグ&ドロップ対応）、実行ボタン、取り扱い注意文 |
| ステータス表示 | 処理中はスピナー＋進捗メッセージを表示 |
| 文字起こしカード | 全文表示エリア＋コピーボタン（処理後に表示） |
| 要約カード | 要約文＋要点リスト＋コピーボタン（処理後に表示） |
| APIキー設定モーダル | 初回起動時、または歯車ボタン押下時に表示 |

## 5. 技術構成

- 単一の `index.html`（HTML／CSS／JavaScriptを1ファイルに集約）
- フレームワーク不使用（Vanilla JS）
- 外部依存：Google Fonts（CDN読み込み）のみ。ビルドツール不要
- ホスティング：GitHub Pages（静的ファイルのみで動作）
- 使用API：Gemini API（`gemini-2.5-flash`）。詳細は `API_NOTES.md` を参照

## 6. 非機能要件・制約

- バックエンドサーバーを持たない（すべてブラウザ→Gemini APIへの直接通信）
- 音声ファイルサイズはGemini APIのインラインデータ上限（リクエスト全体で約20MB）を超えないこと
- 対応音声形式：ブラウザの `accept="audio/*"` で選択可能な形式（mp3, wav, m4a, ogg など。Gemini API側の対応形式に準拠）
- ネットワークエラー・APIエラー時はエラーメッセージを画面に表示し、処理を中断する
