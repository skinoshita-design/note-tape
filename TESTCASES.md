# TESTCASES.md — テスト手順・期待結果・証跡

> 各テストの「証跡」欄には `screenshots/` 内のファイル名を記入する（例：`01_apikey_modal.png`）。
> 実施日・結果（OK/NG）・気づいた点も合わせて記録すること。

## TC01. 初回起動時のAPIキー入力

| 項目 | 内容 |
|---|---|
| 手順 | 1. `localStorage` にキーが無い状態で `index.html` を開く |
| 期待結果 | 自動的にAPIキー入力モーダルが表示される |
| 証跡 | `screenshots/01_apikey_modal.png` |
| 結果 | 未実施 |

## TC02. APIキーの保存・再読み込み後の永続化

| 項目 | 内容 |
|---|---|
| 手順 | 1. モーダルにAPIキーを入力し「保存する」を押す　2. ページをリロードする |
| 期待結果 | モーダルが自動表示されず、歯車ボタンから開くと入力済みキーが表示される |
| 証跡 | `screenshots/02_apikey_saved.png` |
| 結果 | 未実施 |

## TC03. 講義名・音声ファイルの入力（最短導線 手順1〜2）

| 項目 | 内容 |
|---|---|
| 手順 | 1. 講義名を入力する　2. 30秒〜2分程度の音声ファイルを選択する |
| 期待結果 | 選択したファイル名とサイズが表示され、「実行する」ボタンが押せる状態になる |
| 結果 | OK（PC・iPhoneともに確認） |

## TC04. 実行〜文字起こし表示（最短導線 手順3〜4）

| 項目 | 内容 |
|---|---|
| 手順 | 「実行する」を押す |
| 期待結果 | 処理中はスピナーと進捗メッセージが表示され、完了後に文字起こし全文が表示される |
| 結果 | OK（PC・iPhoneともに文字起こしまで成功。iPhoneではAPIレート制限にかかった際もこの段階までは正常動作） |

## TC05. 要約・要点の表示（最短導線 手順5）

| 項目 | 内容 |
|---|---|
| 手順 | TC04に続けて要約処理が自動実行される |
| 期待結果 | 要約文（150〜250字程度）と要点（3〜5個の箇条書き）が表示される |
| 結果 | PCはOK。iPhoneはGemini無料枠のレート制限（429エラー）により未確認、後日再テスト予定（STATUS.md参照） |

## TC06. コピー機能（最短導線 手順6）

| 項目 | 内容 |
|---|---|
| 手順 | 1. 文字起こしのコピーボタンを押す　2. 要約のコピーボタンを押す　3. 任意のテキストエリアに貼り付けて確認する |
| 期待結果 | ボタンが「コピーしました」に変化し、貼り付け内容が画面表示と一致する |
| 証跡 | `screenshots/06_copy_success.png` |
| 結果 | 未実施 |

## TC07. スマホ表示（レスポンシブ確認）

| 項目 | 内容 |
|---|---|
| 手順 | スマートフォン実機、またはブラウザのデベロッパーツールでスマホ幅（375px程度）に切り替えて一連の操作を行う |
| 期待結果 | レイアウト崩れがなく、TC01〜TC06がすべて操作できる |
| 証跡 | `screenshots/07_mobile_view.png` |
| 結果 | OK（iPhone Safari実機、GitHub Pages公開URLで確認。レイアウト崩れなし） |

## TC08. APIキー未入力での実行

| 項目 | 内容 |
|---|---|
| 手順 | APIキーを削除した状態（歯車→「保存済みのキーを削除」）で「実行する」を押す |
| 期待結果 | エラーにならず、APIキー入力モーダルが再表示される |
| 証跡 | `screenshots/08_no_apikey.png` |
| 結果 | 未実施 |

## TC09. 不正なAPIキーでの実行

| 項目 | 内容 |
|---|---|
| 手順 | 無効な文字列をAPIキーとして保存し、「実行する」を押す |
| 期待結果 | 画面にエラーメッセージ（HTTPステータスとGemini APIからのエラー内容）が表示され、アプリが固まらない |
| 証跡 | `screenshots/09_invalid_key_error.png` |
| 結果 | 未実施 |

## TC10. 音声ファイル未選択での実行

| 項目 | 内容 |
|---|---|
| 手順 | ファイルを選択せずに「実行する」を押そうとする |
| 期待結果 | ボタンが非活性（disabled）で押せない、または「音声ファイルを選択してください」というエラーが表示される |
| 証跡 | `screenshots/10_no_file_error.png` |
| 結果 | 未実施 |

## 実施ログ

| 日付 | テストID | 結果 | メモ |
|---|---|---|---|
| 2026-07-27 | TC03〜TC04 | OK | PC・iPhoneともに文字起こしまで成功。iPhoneはファイル選択で複数回つまずき、`.nojekyll`追加・`accept`属性撤廃・ファイル名修正などで解決（詳細はSTATUS.md） |
| 2026-07-27 | TC05 | 一部NG | PCは要約まで成功。iPhoneはGemini無料枠のレート制限（429）に到達し未確認。後日再テスト予定 |
| 2026-07-27 | TC07 | OK | GitHub Pages公開URLでiPhone Safariの表示崩れなしを確認 |
