# STATUS.md — 現在地・詰まりの記録

## 現在地（最終更新：実装完了時点）

- [x] 仕様検討（`SPEC.md`）
- [x] UI実装（講義名入力／音声ファイル選択／実行ボタン／文字起こし表示／要約表示／コピー機能）
- [x] Gemini API連携（文字起こし・要約の2段階呼び出し）
- [x] APIキーの `localStorage` 保存・設定モーダル実装
- [x] レスポンシブ対応（スマホ幅での崩れ確認は要実機テスト）
- [ ] 実際の音声ファイルを使った動作確認（`TESTCASES.md` のTC01〜TC10）
- [ ] スクリーンショットの取得（`screenshots/` に格納）
- [ ] GitHub Pagesへのデプロイと公開URLでの最終確認

## 詰まった点・判断が必要だった点

### 1. 音声文字起こしAPIの選定（Gemini vs Claude）
- 当初、要約以外の全処理もClaude APIに統一することを検討したが、**Claude APIには音声ファイルを直接入力する機能が無い**ことが判明（テキスト・画像・PDFのみ対応）
- 音声→テキスト化を代替する場合、ブラウザの音声認識機能（Web Speech API）でその場録音する方式に変更する必要があり、「音声ファイルアップロード」という課題の最短導線から外れてしまう
- → 音声ファイルアップロードのまま最短導線を満たすため、**文字起こし・要約とも Gemini API（`gemini-2.5-flash`）に統一**する方針で確定（`API_NOTES.md` 参照）

### 2. モデル選定
- 当初検討した `gemini-2.0-flash` は2026年6月1日にシャットダウン済みであることが判明
- 無料枠がありマルチモーダル対応の現行モデル `gemini-2.5-flash` を採用

### 4. 実機テスト中に発生した404エラー（モデルの早期利用不可）
- 実際に音声を投入してテストしたところ、「This model models/gemini-2.5-flash is no longer available to new users」という404エラーが発生
- `gemini-2.5-flash` の公式な廃止予定日は2026年10月16日だったが、7月上旬から新規利用者に対して先行して404が返る事象がGoogle公式フォーラムでも報告されていた
- 公式ドキュメント（音声理解ページ、2026年5月18日更新）で案内されている `gemini-3.5-flash` に切り替えて解決（`API_NOTES.md` に詳細記載）

### 5. iPhone（Safari）で音声ファイルが選択できない
- iPhoneのファイルアプリで音声ファイル（ボイスメモ由来の`.m4a`）がグレーアウトして選択できない事象が発生
  - 原因：`<input type="file" accept="audio/*">` の`accept`指定だけでは、iOS SafariがボイスメモのMIMEタイプを正しく認識できないケースがある
  - 対応：`accept`属性に拡張子指定（`.m4a` など）とMIMEタイプを併記し、対応範囲を広げて解決
- グレーアウトは解消したが、続けて「ファイルを選択→アプリ画面に戻ると未選択扱いになる」という別の事象が発生
  - 原因：ファイル選択エリアを「JavaScriptで隠れたinputをクリックさせる」実装にしていたため、iOS Safariでイベントの取りこぼしが起きていたと推測
  - 対応：ブラウザ標準の `<label for="audioInput">` に選択エリアを置き換え、JSでのクリック誘発をやめてネイティブの挙動に任せる形に変更。あわせて `change` に加えて `input` イベントも監視するようにして取りこぼしに備えた

### 3. APIキーの扱い
- サーバーを持たない構成のため、APIキーはブラウザの `localStorage` に保存し、ブラウザから直接Gemini APIへ送信する方式を採用
- この構成では、デプロイ先を公開URLにした場合、閲覧者が自分自身のAPIキーを入力して使う前提になる（開発者のキーを埋め込んで公開しない）ことを明記

## 次にやること

1. 30秒〜2分程度の（個人情報を含まない）サンプル音声を用意する
2. `TESTCASES.md` のTC01〜TC10を実施し、結果と証跡（スクリーンショット）を記録する
3. GitHub Pagesへデプロイし、公開URLで最終確認する
4. `README.md` の公開URLとリポジトリ情報を更新する
